version: 2

models:
  - name: stg_inventory_customers
    description: inv cust staging table
    columns:
      - name: id
        description: "auto increment primary key"
      - name: first_name
        description: "first name"
      - name: last_name
        description: "last name"
      - name: email
        description: "email"